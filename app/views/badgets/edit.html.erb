<div class="content-wrapper">

  <section class="content-header">
    <h1>予算を編集</h1>
  </section>

  <!-- Main content -->
  <section class="content">
    <div class="row">
      <!-- left column -->
      <div class="col-xs-12">
        <div class="box box-brown">
          <div class="box-body">
            <%= form_with(model: @badget, local: true) do |f| %>
              <%=render 'layouts/error', object: @badget %>


              <div class="form-group">
                <%= f.select :category_id, {"#{@category.kind}":  @category.id}, {}, disabled: true, class: 'form-control' %>
              </div>

              <div class="form-group">
                <div class="input-group">
                  <span class="input-group-addon">金額</span>
                  <%= f.number_field :amount, class: "form-control input-lg", required: true %>
                  <span class="input-group-addon">円</span>
                </div>
              </div>

              <div class="form-group">
                <%= f.submit submit_btn_letters, class: "btn btn-orange btn-block" %>
              </div>

              <%= link_to '削除する', badget_path(@badget), method: :delete, data:{confirm: "Are you sure?"}, class: 'btn btn-brown btn-block' %>

            <% end %>

          </div>
        </div>
      </div>
    </div>
  </section>

</div>