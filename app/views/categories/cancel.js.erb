id = "<%= @category.id %>";
target = document.querySelector(`#category-list-id-${id}`);
html = "<%= j(render partial: 'one_category_list.html.erb', locals: { category: @category }) %>";
target.innerHTML = html;
