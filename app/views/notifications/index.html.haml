.container-fluid
  - @notifications.each do |notification|
    .each_notification
      .notification_msg #{partner.name}さんが#{notification.notification_message.msg}
      - notification.details.each do |key, value|
        %div.notification_detail #{key}: #{value}
        %div.notification_datetime
          = notification.created_at.strftime("%Y/%m/%d(%a)")
